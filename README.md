# BeautyApp (Expo + React Native)

One mobile app that supports both **Customer** and **Partner** roles using JavaScript.

## Setup

```bash
npm install
npx expo start
```

Use Expo Go app and scan QR code.
