const api = {
  get: async () => ({ data: null }),
  post: async () => ({ data: null }),
  put: async () => ({ data: null }),
  patch: async () => ({ data: null }),
  delete: async () => ({ data: null }),
};

export default api;
